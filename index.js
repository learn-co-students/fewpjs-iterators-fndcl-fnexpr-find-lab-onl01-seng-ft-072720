const testVar = {}

function testFunc() {
  return "hi"
}

function superbowlWin(array){
  let solution = array.find(object => object.result === 'W')
  // {
  // return object.year
  // }
  if ( solution !== undefined ){
  return solution.year };
};
